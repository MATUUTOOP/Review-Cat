# test/fixtures/

Shared fixtures, replay golden files, and small seed repositories for tests.

- `replays/` — record/replay golden data
- `configs/` — sample TOML/JSON configs
- `repos/` — tiny git repo seeds or manifests for git tests

Keep fixtures small and scrub secrets before committing.